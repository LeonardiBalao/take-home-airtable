import React from "react";

const TimelineLegend = () => {
  return (
    <div className="mt-4 text-sm text-gray-600">
      <p>Double-click any item to edit its name. Use the zoom slider or the mouse scroll to adjust the timeline scale. (desktop)</p>
    </div>
  );
};

export default TimelineLegend;
